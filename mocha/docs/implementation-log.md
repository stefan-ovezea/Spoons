# Mocha Theme Implementation Log

## 0.1.0

- Added `MochaTheme.spoon` with a Catppuccin Mocha palette copied from `DESIGN.md`.
- Added a configurable top menu bar tint overlay using `hs.canvas`.
- Added screen and wake watchers so overlays rebuild after display changes and wake events.
- Added default bootstrap configuration in `init/mocha.lua`.
